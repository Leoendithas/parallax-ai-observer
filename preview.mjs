import {createServer} from 'node:http';import{readFile}from'node:fs/promises';import{resolve,extname}from'node:path';import worker from './dist/server/index.js';
const base=resolve('dist/client');const types={'.html':'text/html','.js':'text/javascript','.css':'text/css','.png':'image/png','.mp3':'audio/mpeg'};createServer(async(req,res)=>{try{const url=new URL(req.url,'http://127.0.0.1:5174');const request=new Request(url,{method:req.method,headers:req.headers,...(req.method==='POST'?{body:await new Promise(resolve=>{let s='';req.on('data',c=>s+=c);req.on('end',()=>resolve(s));})}:{})});const response=await worker.fetch(request,{OPENAI_API_KEY:process.env.OPENAI_API_KEY,ASSETS:{fetch:async request=>{const path=resolve(base,'.'+(new URL(request.url).pathname==='/'?'/index.html':new URL(request.url).pathname));if(!path.startsWith(base+'/'))return new Response('Not found',{status:404});try{return new Response(await readFile(path),{headers:{'Content-Type':types[extname(path)]||'application/octet-stream'}});}catch{return new Response('Not found',{status:404});}}}});res.writeHead(response.status,Object.fromEntries(response.headers));res.end(Buffer.from(await response.arrayBuffer()));}catch(e){res.writeHead(500);res.end('Preview error');}}).listen(Number(process.env.PORT||5174),'127.0.0.1',()=>console.log('Observer preview http://127.0.0.1:'+(process.env.PORT||5174)));
